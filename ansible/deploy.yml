---
- name: Demo playbook
  hosts: localhost
  tasks:
    - name: Clone app from git
      git:
        repo: 'https://github.com/vanduyms/trello'
        dest: '/ansible'
        clone: yes
        update: yes
    - name: add .env file to backend
      copy:
        src: '/ansible/.env'
        dest: '/ansible/trello/backend'
        mode: '0644'
    - name: Build and start the backend
      shell: |
        cd /ansible/trello/backend
        npm install
        npm run build
        npm run dev
      args:
        chdir: /ansible/trello/backend
    - name: Build and start the frontend
      shell: |
        cd /ansible/trello/frontend
        npm install
        npm run build
        npm run dev
      args:
        chdir: /ansible/trello/frontend